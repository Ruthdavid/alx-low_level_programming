#include "main.h"

/**
 * _abs - checks absolute value of integer
 * @i: parameter to be checked
 *
 * Return: Always 0
 */

int _abs(int) 
{
	if (a < 0)
		a = -(a);
	else if (a >= 0)
		a = a;
	return (a);
}

